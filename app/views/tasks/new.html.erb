<div class="task-new-wrapper" >
  <div class="container">
    <div class="row">
      <div class="col-md-6 col-md-offset-3">
        <h1 class="text-center">Add Task</h1>
        <%= form_for @task do |f| %>
          <div class="form-group">
            <%= f.label :タスク名 %>
            <%= f.text_field :title %>
          </div>

          <div class="form-group">
            <%= f.label :説明 %>
            <%= f.text_area :description, class: 'form-control' %>
          </div>

          <div class="form-group">
            <%= f.label :終了予定日 %>
            <%= f.date_select :finish_schedule_date, class: 'form-control' %>
          </div>

          <div class="form-group">
            <%= f.label :優先度 %>
            <%= f.radio_button :priority,"高", class: 'form-control' %>高<%= f.radio_button :priority,"低", class: 'form-control' %>低
          </div>



          <%= f.submit '保存', class: 'btn btn-black btn-block' %>

        <% end %>


      </div>
    </div>
  </div>
</div>
