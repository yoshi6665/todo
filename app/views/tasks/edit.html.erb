<div class="task-new-wrapper" >
  <div class="container">
    <div class="row">
      <div class="col-md-6 col-md-offset-3">
        <h1 class="text-center">Edit Task</h1>
        <%= form_for(@task, url: task_path(@task), html:{method: :patch}) do |f| %>
          <div class="form-group">
            <%= f.label :title %>
            <%= f.text_field :title, value: @task.title %>
          </div>

          <div class="form-group">
            <%= f.label :description %>
            <%= f.text_area :description, class: 'form-control' , value: @task.description %>
          </div>

          <div class="form-group">
            <%= f.label :終了予定日 %>
            <%= f.date_select :finish_schedule_date, class: 'form-control',value: @task.finish_schedule_date %>
          </div>

          <div class="form-group">
            <%= f.label :優先度 %>
            <%= f.radio_button :priority,"高", class: 'form-control' ,value: @task.priority %>高
            <%= f.radio_button :priority,"低", class: 'form-control' ,value: @task.priority %>低
          </div>

          <div class="form-group">
            <%= f.label :ステータス %>
            <%= f.select :status,[["未着手","未着手"],["処理中","処理中"],["完了","完了"]], class: 'form-control' ,value: @task.status %>
          </div>

          <%= f.submit '保存', class: 'btn btn-black btn-block' %>

        <% end %>


      </div>
    </div>
  </div>
</div>
